# candidates/

Harvested facts awaiting human review. One file per fact (see `../SCHEMA.md`).

A human reviews these, edits as needed, then moves the keepers to `../approved/`
and sets `status: approved` + `approved_by`. Rejects are deleted.
